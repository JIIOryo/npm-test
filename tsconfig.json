{
  "extends": "./tsconfig-base.json",
  "compilerOptions": {
    // .d.tsを生成
    "declaration": true,
    // 別コンポーネントのdirからコードジャンプする時にcoreのtsファイルにジャンプできるようになる
    "declarationMap": true,
    // コンパイル成果物を配置するディレクトリ
    "outDir": "./dist",
    "baseUrl": "./src",
    // protobufの自動生成でjsが吐き出されるためtrueにする
    "allowJs": true,
    "types": [
      "node",
    ],
  },
  "include": [
    "./src/**/*",
    // エディタからテストファイル内の型を効かせるために追加。build時に除外
    "./test/**/*"
  ],
  "exclude": [
    "./dist/",
    "./node_modules/",
  ]
}
